1、声明一个8个长度的整型数组，从键盘获得对应的数字存入数组，正序和倒序输出数组的值。
2、将上题内容进行排序并输出。
3、找出三种排序方式，用中文进行描述。
4、获得一个英文字符串，将所有小写转大写。
5、循环从键盘获得5个字符串，并输出这些字符串是否以abc开头。
6、从键盘获得一个字符串，加密输出。
7、编写程序将 “jdk” 全部变为大写,并输出到屏幕,截取子串”DK” 并输出到屏幕
8、编写程序将String类型字符串”test” 变为 “tset”.
9、去掉一个字符串中空格。
10、(选做)输入一行字符，分别统计出其中英文字母、空格、数字和其它字符的个数。
11、(选做)输入一行字符串(aaabbcc)，去掉重复字符(abc)，并获得每个字符出现的次数(a3b2c2)。
1. 软连接和硬链接的区别， 自己总结一下！

2. 把今天讲解的所有linux命令都手过一遍

3. 搜索整个linux系统下，名字中包含bc，且大小在20k到60k之间的文件
find / -name "*bc*" -a -size +20k -a -size -60k
4. 测试能否为硬链接再创建一个软连接？
可以的
5. 对一个文件中的数字进行排序， 并找出其中的第二个最大数！
cat 1.txt | sed 's/ /\n/g' | grep -v "^$" | sort -nr | sed -n '1p;$p'
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cxz.k_lazy_load.EmpMapper">

    <select id="findSomeById" resultType="Emp" parameterType="int" >
        SELECT * FROM emp
        WHERE did =#{did}
    </select>

</mapper>
server:
  port: 10000
spring:
  application:
    name: customerFeign

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:9000/eureka
  instance:
    prefer-ip-address: true

